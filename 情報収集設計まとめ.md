# JobSimplify Dashboard — 情報収集設計まとめ

> 就活における情報収集・管理の全体設計をまとめたドキュメント。
> 対象: `jobsimplify-dashboard` リポジトリ

---

## 1. 企業トラッキング（カンバンボード）

### 1.1 企業データモデル

`packages/shared/src/types/company.ts` — `Company` インターフェース

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | `string` | UUID |
| `name` | `string` | 企業名 |
| `industry` | `string?` | 業界（19カテゴリ） |
| `status` | `SelectionStatus` | 選考ステータス（11段階） |
| `stages` | `SelectionStage[]` | 各選考ステージの詳細記録 |
| `deadline` | `string?` | レガシー締切フィールド |
| `deadlines` | `CompanyDeadline[]?` | 複数デッドライン |
| `memo` | `string?` | メモ |
| `loginUrl` | `string?` | マイページURL |
| `myPageId` | `string?` | マイページID |
| `loginPassword` | `string?` | マイページパスワード |
| `logoUrl` | `string?` | 企業ロゴURL |
| `websiteDomain` | `string?` | 企業ドメイン |
| `recruitUrl` | `string?` | 採用ページURL |
| `companyMasterId` | `string?` | 企業マスターDB参照ID |
| `createdAt` | `string` | 作成日時 |
| `updatedAt` | `string` | 更新日時 |

**DB構造:** `companies` テーブル（企業メタ情報）+ `applications` テーブル（選考情報: status, stages, deadlines を JSONB で保持）

### 1.2 選考ステータス — 11段階

| 値 | 表示名 | 分類 |
|---|---|---|
| `interested` | 興味あり | 初期 |
| `es_submitted` | ES提出 | 進行中 |
| `webtest` | Webテスト | 選考 |
| `gd` | GD | 選考 |
| `interview_1` | 1次面接 | 選考 |
| `interview_2` | 2次面接 | 選考 |
| `interview_3` | 3次面接 | 選考 |
| `interview_final` | 最終面接 | 選考 |
| `offer` | 内定 | 最終 |
| `rejected` | 不合格 | 終了 |
| `declined` | 辞退 | 終了 |

### 1.3 ステージ詳細記録 (`SelectionStage`)

```typescript
interface SelectionStage {
  type: SelectionStatus;                           // ステータス種別
  date?: string;                                   // YYYY-MM-DD
  time?: string;                                   // HH:mm（24時間）
  result?: 'pending' | 'passed' | 'failed';        // 結果
  memo?: string;                                   // メモ
  customLabel?: string;                            // カスタムラベル
}
```

**新規企業のデフォルトステージ:**
`es_submitted` → `webtest` → `gd` → `interview_1` → `interview_2` → `interview_final`

### 1.4 業界カテゴリ — 19種

| # | カテゴリ |
|---|---|
| 1 | IT・通信 |
| 2 | メーカー（電機・機械） |
| 3 | メーカー（素材・化学） |
| 4 | メーカー（食品・日用品） |
| 5 | 金融（銀行・証券） |
| 6 | 金融（保険） |
| 7 | コンサルティング |
| 8 | 商社（総合） |
| 9 | 商社（専門） |
| 10 | 広告・メディア |
| 11 | エンタメ・ゲーム |
| 12 | 不動産・建設 |
| 13 | インフラ（電力・ガス・鉄道） |
| 14 | 物流・運輸 |
| 15 | 小売・流通 |
| 16 | 人材・教育 |
| 17 | 医療・ヘルスケア |
| 18 | 官公庁・公社・団体 |
| 19 | その他 |

### 1.5 表示モード — 3種

| モード | コンポーネント | 特徴 |
|---|---|---|
| `kanban` | `KanbanBoard.tsx` | ドラッグ&ドロップ、5列表示、@dnd-kit使用 |
| `list` | `ListView.tsx` | ソート可能テーブル（名前・業界・ステータス・更新日） |
| `calendar` | `DeadlineCalendarView.tsx` | 月間カレンダー、デッドライン可視化、Googleカレンダー連携 |

### 1.6 カンバン5列構成

| 列ID | 表示名 | アイコン | 含むステータス |
|---|---|---|---|
| `interested` | 興味あり | ◇ | `interested` |
| `es_submitted` | ES提出 | → | `es_submitted` |
| `selection` | 選考中 | ⟳ | `webtest`, `gd`, `interview_1`〜`interview_final` |
| `offer` | 内定 | ✓ | `offer` |
| `closed` | 不合格 / 辞退 | — | `rejected`, `declined` |

---

## 2. エントリーシート（ES）管理

### 2.1 複合コンテンツモデル

`packages/shared/src/types/entrySheet.ts`

ES は3種類のコンテンツを**同時に**保持できる（非排他的）。`contentType` フィールドは存在せず、各コンテンツの有無で判定する。

#### (a) Q&A 質問 (`questions`)

```typescript
interface ESQuestion {
  id: string;
  entrySheetId: string;
  questionOrder: number;      // 表示順
  questionText: string;       // 質問文
  charLimit?: number;         // 文字数制限
  answer?: string;            // 回答
  createdAt: string;
  updatedAt: string;
}
```

- DB: `es_questions` テーブル（entry_sheet_id で紐付け）
- 進捗率 = 回答済み / 全質問数（0%=灰色, 1-99%=amber, 100%=緑）

#### (b) フリーフォーム (`freeformContent`)

- `freeform_content TEXT` — Tiptap エディタで作成したリッチテキスト（HTML）
- パッケージ: `@tiptap/core`, `@tiptap/react`, `@tiptap/starter-kit`, `@tiptap/extension-placeholder`

#### (c) 外部リンク (`externalLinks`)

```typescript
interface ESExternalLink {
  id: string;
  url: string;
  label: string;
  addedAt: string;
}
```

- DB: `external_links JSONB` — entry_sheets テーブル内に格納

### 2.2 テンプレートシステム

`packages/shared/src/types/template.ts`

**テンプレート種別 (`TemplateType`) — 6種:**

| 値 | 表示名 |
|---|---|
| `selfPR` | 自己PR |
| `gakuchika` | 学生時代に力を入れたこと |
| `strength` | 長所 |
| `weakness` | 短所 |
| `futureGoal` | 将来の目標 |
| `other` | その他 |

**文字数バリエーション:**

```typescript
interface Template {
  id: string;
  type: TemplateType;
  title: string;
  content200?: string;    // 200字版
  content400?: string;    // 400字版
  content600?: string;    // 600字版
  createdAt: string;
  updatedAt: string;
}
```

### 2.3 ファイルマネージャーUI

```
┌────────────────────────────┬──────────────────────────────────┐
│ サイドバー                  │ コンテンツグリッド                │
│                            │                                  │
│ 🔍 検索バー                │ パンくず: ES管理 > [カテゴリ] (N) │
│                            │                                  │
│ ─── TEMPLATES ───          │ ┌──────┐ ┌──────┐ ┌──────┐      │
│ 📁 All Templates (N)       │ │ Card │ │ Card │ │ Card │      │
│                            │ │      │ │      │ │      │      │
│ ─── COMPANIES ───          │ └──────┘ └──────┘ └──────┘      │
│ [+ 企業追加]               │                                  │
│ 🏢 企業A (3)               │ カード表示:                      │
│ 🏢 企業B (1)               │  - タイトル, 企業名              │
│ ...                        │  - 質問数, 進捗バー              │
│                            │  - 更新日時, バージョン(v1,v2..) │
│                            │  - コピー/企業ES化ボタン         │
└────────────────────────────┴──────────────────────────────────┘
```

- **ディープリンク:** `/es?company=<id>` で企業フォルダを直接選択
- **企業ESバージョン:** 作成日順に v1, v2, v3... と自動付与

### 2.4 テンプレート ⇔ 企業ESコピー

**2つのコピーフロー:**

| フロー | トリガー | `requireCompany` | タイトル | 企業選択 |
|---|---|---|---|---|
| 通常コピー | コピーボタン | `false` | `"${title}（コピー）"` | 任意 |
| 企業ES化 | 「企業ESとして使う」 | `true` | 元タイトルのまま | **必須** |

**コピー対象:** 全質問 + フリーフォーム + 外部リンク（全てに新UUID付与）

### 2.5 主要コンポーネント

| コンポーネント | ファイル | 役割 |
|---|---|---|
| ESPage | `src/pages/ESPage.tsx` | メインページ |
| ESFileManagerSidebar | `src/components/ES/ESFileManagerSidebar.tsx` | サイドバーナビ |
| ESFileManagerContent | `src/components/ES/ESFileManagerContent.tsx` | グリッド表示 |
| ESDrawer | `src/components/ES/ESDrawer.tsx` | ES編集ドロワー |
| ESQuestionEditor | `src/components/ES/ESQuestionEditor.tsx` | 質問エディタ（文字数カウント付き） |
| ESCopyModal | `src/components/ES/ESCopyModal.tsx` | コピーモーダル |
| TemplateCreateModal | `src/components/ES/TemplateCreateModal.tsx` | テンプレート作成 |
| TemplateInsertModal | `src/components/ES/TemplateInsertModal.tsx` | テンプレート挿入 |

---

## 3. 締切管理

### 3.1 デッドライン型 — 8種

```typescript
type DeadlineType =
  | 'es_submission'    // ES提出
  | 'internship'       // インターン
  | 'webtest'          // Webテスト
  | 'interview'        // 面接
  | 'offer_response'   // 内定承諾
  | 'document'         // 書類提出
  | 'event'            // 説明会・イベント
  | 'other';           // その他
```

### 3.2 企業別デッドライン (`CompanyDeadline`)

```typescript
interface CompanyDeadline {
  id: string;
  type: DeadlineType;
  label: string;            // カスタムラベル
  date: string;             // YYYY-MM-DD
  time?: string;            // HH:mm
  memo?: string;
  createdAt: string;
  isPreset?: boolean;       // プリセットから追加された場合 true
}
```

- `applications` テーブルの `deadlines JSONB` に格納
- ドロワー内 `DeadlineManager` コンポーネントで CRUD 操作
- Googleカレンダーへのエクスポートボタン付き

### 3.3 デッドラインプリセット

#### マスターDB (`deadline_presets`)

```typescript
interface DeadlinePreset {
  id: string;
  companyMasterId: string;
  recruitmentYear: number;       // 卒業年度（例: 2028）
  deadlineType: DeadlineType;
  label: string;
  deadlineDate: string;          // YYYY-MM-DD
  deadlineTime?: string;         // HH:mm
  memo?: string;
  source: 'admin_curated' | 'community_verified';
  sourceUrl?: string;
  contributedBy?: string;
  verified: boolean;
  contributorCount: number;      // コミュニティ投稿数
  createdAt: string;
  updatedAt: string;
}
```

#### コミュニティ投稿 (`deadline_contributions`)

```typescript
interface DeadlineContribution {
  id: string;
  companyMasterId: string;
  userId: string;
  recruitmentYear: number;
  deadlineType: DeadlineType;
  label: string;
  labelKey: string;              // 正規化ラベル（重複排除用）
  reportedDate: string;
  reportedTime?: string;
  sourceNote?: string;
  status: 'pending' | 'verified' | 'rejected';
  createdAt: string;
}
```

- **Sybil 対策:** アカウント作成 7日未満のユーザーはスキップ
- **自動昇格:** 同一デッドラインに 3人以上が同じ日付を報告 → `deadline_presets` に昇格
- **Upsert キー:** `(company_master_id, user_id, recruitment_year, deadline_type, label_key)`

### 3.4 Googleカレンダー連携

**2つの統合方法:**

| 方法 | 用途 | 実装 |
|---|---|---|
| URL ビルダー | 個別デッドラインをGCalに追加 | `buildGoogleCalendarUrl()` — 新しいタブでGCal作成画面を開く |
| API 読み込み | カレンダー表示にGCalイベントを重畳 | `useGoogleCalendar()` — OAuth2 読み取り専用スコープ |

**URLビルダー仕様:**
- タイトル: `"{企業名} - {deadline.label}"`
- タイムゾーン: `Asia/Tokyo`
- 終日イベント: `YYYYMMDD` 形式（排他的終了日ルール = 翌日）
- 時間指定: `YYYYMMDDTHHMMSS` 形式（1時間枠）

**API仕様:**
- スコープ: `calendar.events.readonly`
- トークン: `localStorage` に保存（有効期限付き）
- 表示: `CalendarEventDisplay` 型に変換し、日付ごとにマッピング

### 3.5 緊急度分類

`src/utils/deadlineHelpers.ts`

| レベル | 条件 | 表示 | カラー |
|---|---|---|---|
| `overdue` | 期限切れ（days < 0） | 「期限切れ」 | 赤 (`bg-red-100 text-red-700`) |
| `urgent` | 0〜3日 | 「今日」「明日」「あとN日」 | 赤系 (`bg-red-50 text-red-600`) |
| `soon` | 4〜7日 | 「あとN日」 | 黄 (`bg-amber-50 text-amber-700`) |
| `normal` | 8日以上 | 「M/D」 | 灰 (`bg-gray-50 text-gray-700`) |

### 3.6 DeadlinePresetsPage

`src/pages/DeadlinePresetsPage.tsx`

**3つの表示モード:**
- **CalendarLayout:** 月間グリッド（7列 = 日〜土）、日別イベントチップ、ポップオーバー詳細
- **WeekLayout:** 7日間ビュー、週ナビゲーション
- **ListView:** 企業別カード、ロゴ・業界・デッドライン一覧

**検索・フィルター:**
- 企業名検索（RPC: `search_deadline_presets`）
- 業界フィルター
- 卒業年度フィルター

**「トラッカーに追加」フロー:**
- プリセット詳細 → 「トラッカーに追加」ボタン → `addCompany()` + トラッカーページへリダイレクト

---

## 4. プロフィール管理

### 4.1 プロフィールデータモデル

`packages/shared/src/types/profile.ts`

#### 基本情報

| フィールド | 型 | 説明 |
|---|---|---|
| `lastName` | `string` | 姓 |
| `firstName` | `string` | 名 |
| `lastNameKana` | `string` | セイ（カタカナ、自動変換） |
| `firstNameKana` | `string` | メイ（カタカナ、自動変換） |
| `lastNameHiragana` | `string` | せい（IME自動取得） |
| `firstNameHiragana` | `string` | めい（IME自動取得） |
| `birthDate` | `string` | 生年月日（YYYY-MM-DD） |
| `gender` | `'male' \| 'female' \| 'other'` | 性別 |

#### 連絡先

| フィールド | 型 | 説明 |
|---|---|---|
| `email` | `string` | メールアドレス |
| `phoneNumber` | `string` | 携帯電話番号（数字のみ） |
| `homePhoneNumber` | `string?` | 自宅電話番号 |

#### 住所

| フィールド | 型 | 説明 |
|---|---|---|
| `postalCode` | `string` | 郵便番号（7桁で自動入力発火） |
| `prefecture` | `string` | 都道府県 |
| `city` | `string` | 市区町村 |
| `address1` | `string` | 町名・番地 |
| `address2` | `string?` | 建物名・部屋番号 |

#### 休暇中連絡先（帰省先）

| フィールド | 型 | 説明 |
|---|---|---|
| `holidayPostalCode` | `string?` | 郵便番号 |
| `holidayPrefecture` | `string?` | 都道府県 |
| `holidayCity` | `string?` | 市区町村 |
| `holidayAddress1` | `string?` | 町名・番地 |
| `holidayAddress2` | `string?` | 建物名・部屋番号 |
| `holidayPhoneNumber` | `string?` | 電話番号 |

#### 学歴

| フィールド | 型 | 説明 |
|---|---|---|
| `university` | `string` | 大学名 |
| `faculty` | `string` | 学部 |
| `department` | `string` | 学科 |
| `graduationYear` | `number` | 卒業予定年 |
| `graduationMonth` | `number` | 卒業予定月（デフォルト: 3） |
| `entranceYear` | `number?` | 入学年 |
| `entranceMonth` | `number?` | 入学月 |
| `highSchool` | `string?` | 出身高校 |
| `highSchoolGradYear` | `number?` | 高校卒業年 |
| `highSchoolGradMonth` | `number?` | 高校卒業月 |

#### 資格・スキル

| フィールド | 型 | 説明 |
|---|---|---|
| `toeicScore` | `number?` | TOEICスコア（0〜990） |
| `toeicDate` | `string?` | TOEIC受験日（YYYY-MM） |
| `toeflScore` | `number?` | TOEFLスコア（0〜120） |
| `driverLicense` | `string?` | 運転免許 |
| `qualifications` | `Qualification[]?` | 資格一覧（動的配列） |

```typescript
interface Qualification {
  name: string;    // 資格名
  date: string;    // 取得年月（YYYY-MM）
}
```

#### その他

| フィールド | 型 | 説明 |
|---|---|---|
| `hobby` | `string?` | 趣味 |
| `club` | `string?` | 部活・サークル |
| `seminarLab` | `string?` | ゼミ・研究室 |
| `researchTheme` | `string?` | 研究テーマ |

### 4.2 郵便番号 → 住所自動入力

`src/pages/ProfilePage.tsx`

- **API:** `zipcloud.ibsnet.co.jp`
- **発火条件:** 郵便番号が7桁に達した時点
- **マッピング:** `address1` ← API result, `address2` ← city, `address3` ← address1
- **キャンセル処理:** レースコンディション回避
- **対応:** 通常住所 + 帰省先住所の両方

### 4.3 IME ふりがな自動取得

`src/pages/ProfilePage.tsx` — `useIMEFurigana` カスタムフック

**仕組み:**
1. `compositionstart` → 入力開始
2. `compositionupdate` → IME中間データからひらがなを抽出
3. `compositionend` → `lastNameHiragana` / `firstNameHiragana` に反映
4. ひらがな → カタカナ自動変換（`hiraganaToKatakana()`）

```typescript
// ひらがな抽出
function extractHiragana(str: string): string {
  return str.replace(/[^\u3041-\u3096]/g, '');
}

// ひらがな → カタカナ変換
function hiraganaToKatakana(str: string): string {
  return str.replace(/[\u3041-\u3096]/g, (match) =>
    String.fromCharCode(match.charCodeAt(0) + 0x60)
  );
}
```

### 4.4 DB保存

- **テーブル:** `user_profiles` — `profile_data JSONB` に Profile オブジェクト全体を格納
- **操作:** `getProfile()` / `saveProfile()` のみ（upsert）

---

## 5. データ基盤

### 5.1 Supabase + Chrome Storage フォールバック

```
認証済みユーザー → Supabase (PostgreSQL)
未認証ユーザー  → Chrome Storage API / localStorage
```

**判定ロジック:** 各リポジトリの先頭で認証チェック → 未認証時はローカルストレージへフォールバック

**ローカルストレージキー:**
- `profile` — プロフィールデータ
- `templates` — ESテンプレート
- `companies` — 企業データ
- `entry_sheets` — エントリーシート
- `settings` — ユーザー設定
- `autofill_logs` — 自動入力ログ（最大50件）
- `jobsimplify:gcal-enabled` — Googleカレンダー有効フラグ
- `jobsimplify:gcal-token` — GCalトークン（有効期限付き）

### 5.2 Supabase テーブル一覧

| テーブル | 用途 |
|---|---|
| `profiles` | Supabase Auth 連携 |
| `user_profiles` | プロフィールデータ（JSONB） |
| `companies` | 企業メタ情報 |
| `applications` | 選考状況（status, stages, deadlines を JSONB で保持） |
| `templates` | ESテンプレート |
| `entry_sheets` | エントリーシート |
| `es_questions` | ES質問（entry_sheet_id で紐付け） |
| `deadline_contributions` | コミュニティ締切投稿 |
| `deadline_presets` | 締切プリセットマスター |

### 5.3 企業マスターDB

**RPC関数:** `search_company_master(search_query, result_limit)`

**検索結果型 (`CompanySearchResult`):**

| フィールド | 説明 |
|---|---|
| `id` | マスターID |
| `name` | 企業名 |
| `nameKana` | 企業名カナ |
| `industry` | 業界 |
| `logoUrl` | ロゴURL |
| `websiteUrl` | 企業サイトURL |
| `websiteDomain` | ドメイン |
| `mypageUrl` | マイページURL |
| `recruitUrl` | 採用ページURL |
| `isPopular` | 人気企業フラグ |
| `similarityScore` | 類似度スコア |

**企業追加時の自動入力フロー:**
1. 企業名入力 → デバウンス300ms → RPC検索
2. 候補選択 →
   - 業界: `mapMasterIndustry()` でキーワードマッチング（保険→金融(保険)、SIer→IT・通信 等）
   - ドメイン: `normalizeWebsiteDomain()` でプロトコル除去
   - 採用URL: マスターデータから直接設定
   - デッドライン: `getPresetsByMasterId()` でプリセット取得 → `isPreset: true` で登録

### 5.4 リポジトリパターン

`packages/shared/src/repositories/`

| リポジトリ | 対象テーブル | 主要操作 |
|---|---|---|
| `applicationRepository` | `companies` + `applications` | CRUD（企業・選考） |
| `entrySheetRepository` | `entry_sheets` + `es_questions` | CRUD（ES・質問） |
| `templateRepository` | `templates` | CRUD（テンプレート） |
| `profileRepository` | `user_profiles` | Get / Save（upsert） |
| `deadlinePresetRepository` | `deadline_presets` | 検索・取得（読み取り専用） |
| `deadlineContributionRepository` | `deadline_contributions` | 投稿・取得 |
| `adminRepository` | 管理者用 | 承認・拒否・再計算 |

### 5.5 snake_case ⇔ camelCase 変換

`packages/shared/src/types/database.ts`

全DB行は `snake_case`、全アプリ型は `camelCase`。変換は明示的な関数で集中管理。

**主要変換関数:**

| 関数 | 方向 | 対象 |
|---|---|---|
| `dbToCompany()` | DB → App | companies + applications → Company |
| `dbTemplateToTemplate()` | DB → App | templates → Template |
| `templateToDbInsert()` | App → DB | Template → Insert |
| `dbEntrySheetToEntrySheet()` | DB → App | entry_sheets → EntrySheet |
| `entrySheetToDbInsert()` | App → DB | EntrySheet → Insert |
| `dbESQuestionToESQuestion()` | DB → App | es_questions → ESQuestion |
| `esQuestionToDbInsert()` | App → DB | ESQuestion → Insert |

**変換例:**
```
login_url       ⇔ loginUrl
my_page_id      ⇔ myPageId
company_master_id ⇔ companyMasterId
freeform_content ⇔ freeformContent
external_links   ⇔ externalLinks
question_order   ⇔ questionOrder
char_limit       ⇔ charLimit
content_200      ⇔ content200
```

---

## 付録: アーキテクチャ概要図

```
┌─────────────────────────────────────────────────────────┐
│                    React Dashboard (src/)                 │
│                                                          │
│  TrackerPage ──── ESPage ──── DeadlinePresetsPage ──── ProfilePage │
│      │              │              │                    │ │
│  KanbanBoard   ESDrawer    CalendarLayout         useIMEFurigana │
│  ListView      ESCopyModal WeekLayout             zipcloud API │
│  CalendarView  ESQuestion  ListView               │ │
│      │              │              │                    │ │
│  ┌───▼──────────────▼──────────────▼────────────────────▼─┐ │
│  │          Context / Hooks Layer                         │ │
│  │  useCompanies  useEntrySheets  useDeadlinePresets      │ │
│  │  EntrySheetContext  useGoogleCalendar                  │ │
│  └───────────────────────┬───────────────────────────────┘ │
└──────────────────────────┼──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│              packages/shared/                                │
│                                                              │
│  types/          repositories/           utils/              │
│  company.ts      applicationRepo.ts      formatters.ts       │
│  entrySheet.ts   entrySheetRepo.ts       deadlineHelpers.ts  │
│  template.ts     templateRepo.ts                             │
│  profile.ts      profileRepo.ts                              │
│  database.ts     deadlinePresetRepo.ts                       │
│  deadlinePreset  deadlineContribRepo.ts                      │
│                  adminRepo.ts                                │
└──────────────────────────┬──────────────────────────────────┘
                           │
              ┌────────────┴────────────┐
              │                         │
     ┌────────▼────────┐    ┌──────────▼──────────┐
     │    Supabase      │    │   Chrome Storage    │
     │   (PostgreSQL)   │    │   / localStorage    │
     │                  │    │                     │
     │ companies        │    │ profile             │
     │ applications     │    │ companies           │
     │ entry_sheets     │    │ entry_sheets        │
     │ es_questions     │    │ templates           │
     │ templates        │    │ settings            │
     │ user_profiles    │    │                     │
     │ deadline_presets │    │                     │
     │ deadline_contribs│    │                     │
     │ company_master   │    │                     │
     └─────────────────┘    └─────────────────────┘
```
